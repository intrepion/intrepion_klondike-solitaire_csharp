@inject ICardFoundationFilters Filters

@if (Column == Filters.SortColumn)
{
    <span>@SortSymbol</span>
}
else
{
    <span>&nbsp;</span>
}

@code {
    // Sets which CardFoundationFilterColumns this indicator is for.
    [Parameter]
    public CardFoundationFilterColumns Column { get; set; }

    // The symbol to render.
    private string SortSymbol => Filters.SortAscending ? "🔼" : "🔽";
}
